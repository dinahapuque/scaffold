import { takeEvery } from 'redux-saga/effects';
import {
  DEFAULT_ACTION,
} from "../actions/types";


export default function* rootSaga() {
  
  // yield takeEvery(DEFAULT_ACTION, defaultSuccess);
}